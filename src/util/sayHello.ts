// Temporary function so that tsc doesn't chuck the hello lambda just inside of dist
// DELETE THIS FILE once we have more util or something else please
export function sayHello() {
    return "Hello";
}